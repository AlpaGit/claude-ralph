import type { JSX } from "react";

/**
 * SettingsView -- model configuration and app preferences.
 * Route: /settings
 */
export function SettingsView(): JSX.Element {
  return (
    <section className="view-stub">
      <h2>Settings</h2>
      <p>Settings panel will be rendered here.</p>
    </section>
  );
}
